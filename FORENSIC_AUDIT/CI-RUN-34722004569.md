# CI evidence: run 34722004569

Source: GitHub Actions run for `jeridj1/DroidVibe`, workflow CI, run number 4355.

Observed run date: 2026-09-12.

PR context: pull request #122, branch `fix/android-toolchain-symlink-resolution-main`.

Head commit used by the PR-triggered run: `087b9ef5c2cb0480224642a0592a498fe4b2e940`.
The workflow checked out merge ref `087315a0e40918117623116e67a74eec9565c294`, described in the log as the merge of the PR head into main.

## Job results

Typecheck and Lint: SUCCESS.

Build Web Backend: SUCCESS.

Unit Tests: SUCCESS.

Expo Prebuild Validation: SUCCESS.

Build ARM64 Arduino Toolchain: FAILURE.

Build Android APK: SKIPPED because the toolchain job failed.

## High-value log sequence

The ARM64 runner was Ubuntu 24.04.5 LTS, image `ubuntu-24.04-arm`, ARM64.

The toolchain build successfully installed and listed:

`arduino:avr 1.8.8`

`arduino:megaavr 1.8.8`

`rp2040:rp2040 6.1.0`

The downloaded proot was identified as an ARM aarch64 statically linked executable.

The self-test then successfully compiled an Uno sketch, a Mega sketch, and an RP2040 Pico sketch. It printed:

`LOCAL_TOOLCHAIN_PRROOT_TEST_OK`

It also printed Arduino CLI version `1.5.1` and a proot SHA256 of `fa10b1a7818c2f5b1dcb5834450570c368c9ecf66d31521509621b95c4538a45`.

Only after all of that, archive creation/splitting reached the host-side permission step and failed:

`chmod: changing permissions of '/home/runner/work/_temp/droidvibe-assets/droidvibe-toolchain-rootfs.tar.gz': Operation not permitted`

The job exited with code one. The subsequent upload step was skipped.

## Interpretation boundary

The log proves that the toolchain itself passed the included ARM64/proot compilation self-test in this run. It does not prove that the resulting archive would install and compile correctly on an Android phone. It also does not prove that PR #122 fixes the issue, because the PR remained draft/open and the fix was not merged into main at the time of this evidence snapshot.

The log contains an additional runner warning that GitHub Actions was forcing Node twenty-four for actions targeting deprecated Node twenty. Treat that as a maintenance/configuration observation, not the cause of this toolchain failure.
